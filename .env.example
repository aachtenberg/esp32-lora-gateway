# MQTT Broker Configuration
# Copy this file to .env and configure for your setup

# MQTT Broker address (hostname or IP)
MQTT_BROKER=localhost

# MQTT Broker port (default: 1883)
MQTT_PORT=1883

# MQTT Authentication (leave empty if not required)
MQTT_USER=
MQTT_PASS=

# Default sensor device ID (full 16-character hex from sensor)
# Get this from sensor serial output or MQTT messages
DEFAULT_DEVICE_ID=AABBCCDDEEFF0011

# PostgreSQL Database Configuration
PG_HOST=192.168.x.x
PG_PORT=5432
PG_DATABASE=iot_sensors
PG_USER=your_username
PG_PASSWORD=your_password_here
